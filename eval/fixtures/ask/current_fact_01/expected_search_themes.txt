The digest mentions 4.1% but the user asks for "current" — the search should look for the latest ten-year Treasury yield, not just rely on the digest value. Tests whether the model recognizes "current" as needing a fresh search rather than echoing the digest figure.