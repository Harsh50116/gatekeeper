Must resolve "this region" to Valparaiso / central Chile from conversation history and digest. The search query must include "Chile" or "Valparaiso" — without digest grounding, the query would be too vague ("has this region had major earthquakes" is meaningless to Tavily). Tests that the model embeds digest-specific details into the search query to make it precise. If the query is generic like "major earthquakes region history", the digest grounding failed.